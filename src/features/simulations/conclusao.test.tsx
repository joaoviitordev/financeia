// Esqueleto de spec da feature resultado-persistido (T-005).
// A tag @spec:AC-xxx no título liga o teste à especificação.
import { test } from 'vitest';

// US-004 — Página de resultado com link permanente
test('AC-010: Concluir leva ao resultado da simulação criada @spec:AC-010', () => {
  // Dado: que respondi todas as perguntas
  // Quando: confirmo a última
  // Então: sou levada ao endereço de resultado da simulação recém-guardada
  throw new Error('critério de aceite AC-010 ainda não provado — implemente este teste');
});
